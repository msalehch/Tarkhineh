
function NotFound() {

    return(
        <div>
            NotFound
            <button type="button" onClick={() => {history.back()}}>back</button>
        </div>

    )
}
export default NotFound;